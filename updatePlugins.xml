<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="com.jetbrains.lightThemePreview" url="https://raw.githubusercontent.com/bhanafee/jbplugins/master/IntelliJLightTheme.jar" version="1.0.1">
    <!--
      The <idea-version> element must match the same element in plugin.xml. Required.
    -->
    <idea-version since-build="191.0" until-build="193"/>
  </plugin>
  <plugin id="com.example.customThemeColor" url="https://www.example.com/not/real.jar" version="1.0.0">
    <!--
      The <idea-version> element must match the same element in plugin.xml. Required.
    -->
    <idea-version since-build="191.0" until-build="193"/>
  </plugin>
</plugins>